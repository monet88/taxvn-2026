---
phase: 03-mobile-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["apps/mobile/app.json", "apps/mobile/package.json", "apps/mobile/metro.config.js", "apps/mobile/tailwind.config.ts", "apps/mobile/global.css", "apps/mobile/app/_layout.tsx", "apps/mobile/app/(tabs)/_layout.tsx", "apps/mobile/app/(tabs)/index.tsx", "apps/mobile/app/(tabs)/so-sanh.tsx", "apps/mobile/app/(tabs)/tham-khao.tsx", "apps/mobile/app/(tabs)/tai-khoan.tsx"]
autonomous: true
requirements: ["UX-01", "UX-09", "UX-11"]
must_haves:
  truths:
    - "User can open the app and see a 4-tab layout"
    - "UI uses NativeWind utility classes (e.g., bg-background, text-primary)"
  artifacts:
    - path: "apps/mobile/app/(tabs)/_layout.tsx"
      provides: "Main tab navigation"
    - path: "apps/mobile/tailwind.config.ts"
      provides: "Design tokens for mobile"
  key_links:
    - from: "apps/mobile/app/_layout.tsx"
      to: "apps/mobile/global.css"
      via: "import"
      pattern: "import './global.css'"
---

<objective>
Initialize the Expo Router shell and configure the NativeWind v4 design system.

Purpose: Establishes the core navigational and visual foundation of the mobile application before integrating state or authentication.
Output: A working 4-tab Expo app styled with Tailwind utility classes.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-mobile-foundation/03-CONTEXT.md
@.planning/phases/03-mobile-foundation/03-RESEARCH.md
@.planning/phases/03-mobile-foundation/03-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap Expo App and Testing Infra (Wave 0)</name>
  <files>apps/mobile/app.json, apps/mobile/package.json, apps/mobile/jest.config.js</files>
  <action>Create the 'mobile' directory under 'apps/' if it doesnt exist using `npx create-expo-app@latest mobile --template tabs`. Then move it inside `apps/`. Update the package.json name to `@taxvn/mobile`. Install `jest-expo` and `@testing-library/react-native`. Create `jest.config.js` for Expo. Run `pnpm install` at the root.</action>
  <verify>
    <automated>pnpm --filter @taxvn/mobile exec jest --passWithNoTests</automated>
  </verify>
  <done>Expo app is initialized in apps/mobile with basic testing infra.</done>
</task>

<task type="auto">
  <name>Task 2: Configure NativeWind v4</name>
  <files>apps/mobile/package.json, apps/mobile/metro.config.js, apps/mobile/tailwind.config.ts, apps/mobile/global.css, apps/mobile/app/_layout.tsx, apps/mobile/babel.config.js</files>
  <action>Install `nativewind@^4.2.3` and `tailwindcss@^3.4.17` in the mobile app. Setup `metro.config.js` using `withNativeWind`. Create `tailwind.config.ts` mapping the tokens from `03-UI-SPEC.md` (colors, spacing, typography). Create `global.css` with Tailwind directives and import it in `app/_layout.tsx`. Configure `babel.config.js` properly for Expo Router and Reanimated as per NativeWind v4 docs.</action>
  <verify>
    <automated>cat apps/mobile/tailwind.config.ts | grep content</automated>
  </verify>
  <done>NativeWind configuration is fully applied and global.css is imported.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement 4-Tab Navigation</name>
  <files>apps/mobile/app/(tabs)/_layout.tsx, apps/mobile/app/(tabs)/index.tsx, apps/mobile/app/(tabs)/so-sanh.tsx, apps/mobile/app/(tabs)/tham-khao.tsx, apps/mobile/app/(tabs)/tai-khoan.tsx, apps/mobile/__tests__/navigation.test.tsx</files>
  <behavior>
    - Test 1: Tab layout renders 4 specific tabs (Tính Thuế, So Sánh, Tham Khảo, Tài Khoản)
  </behavior>
  <action>Create the 4 main tabs as defined in UX-01 inside `app/(tabs)/`. Style the tab bar using NativeWind classes (e.g. `className` on Tab Bar components or Screen options) reflecting the brand color (`#059669`) for active tints. Delete the default boilerplate screens generated by `create-expo-app`. Ensure `index.tsx` maps to the Tinh Toan tab.</action>
  <verify>
    <automated>pnpm --filter @taxvn/mobile test -- navigation</automated>
  </verify>
  <done>4-tab layout is rendering and passing basic component tests.</done>
</task>

</tasks>

<success_criteria>
The mobile app builds successfully, renders a 4-tab layout, and correctly parses NativeWind v4 tailwind classes.
</success_criteria>

<output>
After completion, create `.planning/phases/03-mobile-foundation/03-01-SUMMARY.md`
</output>
