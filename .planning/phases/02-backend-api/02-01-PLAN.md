---
phase: 02
plan: 01
title: "Supabase Project Setup & Database Schema"
wave: 1
depends_on: []
files_modified:
  - supabase/config.toml
  - supabase/migrations/00001_initial_schema.sql
  - supabase/.env.example
  - apps/api/package.json
  - apps/api/src/supabase.ts
  - docker-compose.yml
  - package.json
autonomous: true
requirements_addressed: [API-01, API-02, API-03, API-04, SEC-01, SEC-02, OBS-01, OBS-02]
---

# Plan 02-01: Supabase Project Setup & Database Schema

<objective>
Initialize Supabase project with CLI, create SQL migrations for all custom tables (calculation_history, share_snapshots, push_tokens, app_config) with Row Level Security policies, configure Supabase Auth for email + Google OAuth, and create the shared Supabase client package. Verify local Supabase dev environment works.
</objective>

<tasks>

<task id="01-01" title="Initialize Supabase project">
<read_first>
- pnpm-workspace.yaml (verify apps/* glob)
- package.json (root package.json for scripts)
</read_first>
<action>
Run `npx supabase init` from project root to create `supabase/` directory.

This creates:
- `supabase/config.toml` — local dev configuration
- `supabase/migrations/` — SQL migration directory
- `supabase/functions/` — Edge Functions directory
- `supabase/seed.sql` — seed data (optional)

Update root `package.json` scripts:
```json
{
  "scripts": {
    "supabase:start": "supabase start",
    "supabase:stop": "supabase stop",
    "supabase:reset": "supabase db reset",
    "supabase:migrate": "supabase migration new",
    "supabase:functions": "supabase functions serve"
  }
}
```

Create `supabase/.env.example`:
```
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
```

Update `.gitignore` to add:
```
# Supabase local
supabase/.temp/
```
</action>
<acceptance_criteria>
- `supabase/config.toml` exists
- `supabase/migrations/` directory exists
- `supabase/functions/` directory exists
- `supabase/.env.example` contains `SUPABASE_URL`
- Root `package.json` contains `"supabase:start"`
</acceptance_criteria>
</task>

<task id="01-02" title="SQL migration: custom tables with RLS">
<read_first>
- .planning/REQUIREMENTS.md (API-02 schema draft lines 26-31)
- .planning/phases/02-backend-api/02-CONTEXT.md (data model decisions)
</read_first>
<action>
Create `supabase/migrations/00001_initial_schema.sql`:

```sql
-- ============================================================
-- TaxVN Backend Schema
-- Auth tables managed by Supabase Auth (auth.users, auth.sessions)
-- Custom tables below for app-specific data
-- ============================================================

-- Lịch sử tính thuế
CREATE TABLE calculation_history (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
  tool_name TEXT NOT NULL,
  input_json JSONB NOT NULL,
  result_json JSONB NOT NULL,
  snapshot_version INTEGER NOT NULL DEFAULT 1,
  tax_core_version TEXT NOT NULL,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

-- Index cho truy vấn thường xuyên
CREATE INDEX idx_history_user_created ON calculation_history(user_id, created_at DESC);
CREATE INDEX idx_history_user_tool ON calculation_history(user_id, tool_name);

-- Chia sẻ kết quả tính thuế
CREATE TABLE share_snapshots (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  token VARCHAR(8) NOT NULL UNIQUE,
  snapshot_json JSONB NOT NULL,
  snapshot_version INTEGER NOT NULL DEFAULT 1,
  tax_core_version TEXT NOT NULL,
  created_by UUID REFERENCES auth.users(id) ON DELETE SET NULL,
  expires_at TIMESTAMPTZ NOT NULL,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE INDEX idx_share_token ON share_snapshots(token);
CREATE INDEX idx_share_expires ON share_snapshots(expires_at);

-- Push notification tokens
CREATE TABLE push_tokens (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
  token TEXT NOT NULL,
  platform TEXT NOT NULL CHECK (platform IN ('ios', 'android')),
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  UNIQUE(user_id, token)
);

-- Cấu hình app (version gate, feature flags)
CREATE TABLE app_config (
  key TEXT PRIMARY KEY,
  value JSONB NOT NULL,
  updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

-- Seed version config
INSERT INTO app_config (key, value) VALUES
  ('version_gate', '{
    "minimumVersion": "0.1.0",
    "latestVersion": "0.1.0",
    "effectiveDate": "2026-01-01",
    "disabledCalculators": [],
    "updateUrl": {
      "ios": "https://apps.apple.com/app/taxvn/id000000000",
      "android": "https://play.google.com/store/apps/details?id=io.1devops.taxvn"
    }
  }'::jsonb);

-- ============================================================
-- Row Level Security (RLS)
-- ============================================================

ALTER TABLE calculation_history ENABLE ROW LEVEL SECURITY;
ALTER TABLE share_snapshots ENABLE ROW LEVEL SECURITY;
ALTER TABLE push_tokens ENABLE ROW LEVEL SECURITY;
ALTER TABLE app_config ENABLE ROW LEVEL SECURITY;

-- Calculation History: user chỉ thấy data của mình
CREATE POLICY "Users can insert own history"
  ON calculation_history FOR INSERT
  WITH CHECK (auth.uid() = user_id);

CREATE POLICY "Users can view own history"
  ON calculation_history FOR SELECT
  USING (auth.uid() = user_id);

CREATE POLICY "Users can delete own history"
  ON calculation_history FOR DELETE
  USING (auth.uid() = user_id);

-- Share Snapshots: ai cũng đọc được (public read by token), chỉ auth user tạo được
CREATE POLICY "Anyone can read share by token"
  ON share_snapshots FOR SELECT
  USING (expires_at > now());

CREATE POLICY "Authenticated users can create shares"
  ON share_snapshots FOR INSERT
  WITH CHECK (true);  -- Edge Function dùng service_role key

-- Push Tokens: user quản lý token của mình
CREATE POLICY "Users can manage own push tokens"
  ON push_tokens FOR ALL
  USING (auth.uid() = user_id)
  WITH CHECK (auth.uid() = user_id);

-- App Config: public read, no write (admin via dashboard)
CREATE POLICY "Anyone can read app config"
  ON app_config FOR SELECT
  USING (true);

-- ============================================================
-- Functions
-- ============================================================

-- Auto-update updated_at
CREATE OR REPLACE FUNCTION update_updated_at()
RETURNS TRIGGER AS $$
BEGIN
  NEW.updated_at = now();
  RETURN NEW;
END;
$$ LANGUAGE plpgsql;

CREATE TRIGGER set_push_tokens_updated_at
  BEFORE UPDATE ON push_tokens
  FOR EACH ROW
  EXECUTE FUNCTION update_updated_at();
```
</action>
<acceptance_criteria>
- `supabase/migrations/00001_initial_schema.sql` contains `CREATE TABLE calculation_history`
- `supabase/migrations/00001_initial_schema.sql` contains `CREATE TABLE share_snapshots`
- `supabase/migrations/00001_initial_schema.sql` contains `CREATE TABLE push_tokens`
- `supabase/migrations/00001_initial_schema.sql` contains `CREATE TABLE app_config`
- `supabase/migrations/00001_initial_schema.sql` contains `ENABLE ROW LEVEL SECURITY`
- `supabase/migrations/00001_initial_schema.sql` contains `auth.uid()`
- `supabase db reset` applies migration successfully (local Supabase)
</acceptance_criteria>
</task>

<task id="01-03" title="Supabase client package for shared usage">
<read_first>
- packages/tax-core/package.json (reference workspace package pattern)
- pnpm-workspace.yaml
</read_first>
<action>
Create `apps/api/package.json`:
```json
{
  "name": "@taxvn/supabase",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "dependencies": {
    "@supabase/supabase-js": "^2.49.0"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  }
}
```

Create `apps/api/src/supabase.ts`:
```typescript
import { createClient } from '@supabase/supabase-js';
import type { Database } from './database.types.js';

const supabaseUrl = process.env.SUPABASE_URL ?? process.env.EXPO_PUBLIC_SUPABASE_URL ?? '';
const supabaseAnonKey = process.env.SUPABASE_ANON_KEY ?? process.env.EXPO_PUBLIC_SUPABASE_ANON_KEY ?? '';

export const supabase = createClient<Database>(supabaseUrl, supabaseAnonKey);

export type { Database };
```

Create `apps/api/src/database.types.ts` — TypeScript types generated from schema (placeholder, will be generated with `supabase gen types typescript`):
```typescript
export interface Database {
  public: {
    Tables: {
      calculation_history: {
        Row: {
          id: string;
          user_id: string;
          tool_name: string;
          input_json: Record<string, unknown>;
          result_json: Record<string, unknown>;
          snapshot_version: number;
          tax_core_version: string;
          created_at: string;
        };
        Insert: Omit<Database['public']['Tables']['calculation_history']['Row'], 'id' | 'created_at'> & {
          id?: string;
          created_at?: string;
        };
        Update: Partial<Database['public']['Tables']['calculation_history']['Insert']>;
      };
      share_snapshots: {
        Row: {
          id: string;
          token: string;
          snapshot_json: Record<string, unknown>;
          snapshot_version: number;
          tax_core_version: string;
          created_by: string | null;
          expires_at: string;
          created_at: string;
        };
        Insert: Omit<Database['public']['Tables']['share_snapshots']['Row'], 'id' | 'created_at'> & {
          id?: string;
          created_at?: string;
        };
        Update: Partial<Database['public']['Tables']['share_snapshots']['Insert']>;
      };
      push_tokens: {
        Row: {
          id: string;
          user_id: string;
          token: string;
          platform: 'ios' | 'android';
          created_at: string;
          updated_at: string;
        };
        Insert: Omit<Database['public']['Tables']['push_tokens']['Row'], 'id' | 'created_at' | 'updated_at'> & {
          id?: string;
          created_at?: string;
          updated_at?: string;
        };
        Update: Partial<Database['public']['Tables']['push_tokens']['Insert']>;
      };
      app_config: {
        Row: {
          key: string;
          value: Record<string, unknown>;
          updated_at: string;
        };
        Insert: Database['public']['Tables']['app_config']['Row'];
        Update: Partial<Database['public']['Tables']['app_config']['Insert']>;
      };
    };
  };
}
```

Create `apps/api/src/index.ts`:
```typescript
export { supabase } from './supabase.js';
export type { Database } from './database.types.js';
```

Note: After Supabase local is running, regenerate types with:
```bash
supabase gen types typescript --local > apps/api/src/database.types.ts
```
</action>
<acceptance_criteria>
- `apps/api/package.json` contains `"@supabase/supabase-js"`
- `apps/api/src/supabase.ts` contains `createClient<Database>`
- `apps/api/src/database.types.ts` contains `calculation_history`
- `apps/api/src/database.types.ts` contains `share_snapshots`
- `apps/api/src/database.types.ts` contains `push_tokens`
- `apps/api/src/database.types.ts` contains `app_config`
- `apps/api/src/index.ts` exports `supabase`
</acceptance_criteria>
</task>

</tasks>

<verification>
After all tasks complete:
1. `supabase start` runs local Supabase instance (Docker required)
2. `supabase db reset` applies migration without errors
3. Supabase Studio at `http://localhost:54323` shows all 4 custom tables
4. RLS policies visible in Supabase Studio → Authentication → Policies
5. `app_config` table contains `version_gate` seed data
6. TypeScript types compile: `tsc --noEmit` from apps/api/
</verification>

<must_haves>
- Supabase project initialized with CLI
- 4 custom tables with proper indexes (calculation_history, share_snapshots, push_tokens, app_config)
- RLS policies: user-scoped history/push, public-read shares, public-read config
- Supabase Auth configured (email + Google — dashboard setup)
- Shared TypeScript types for database schema
- Local dev environment functional via `supabase start`
</must_haves>
