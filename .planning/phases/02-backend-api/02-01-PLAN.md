---
phase: 02
plan: 01
title: "API Scaffold & Database Schema"
wave: 1
depends_on: []
files_modified:
  - apps/api/package.json
  - apps/api/tsconfig.json
  - apps/api/prisma/schema.prisma
  - apps/api/prisma/prisma.config.ts
  - apps/api/src/index.ts
  - apps/api/src/server.ts
  - apps/api/src/trpc.ts
  - apps/api/src/context.ts
  - apps/api/src/env.ts
  - apps/api/.env.example
  - docker-compose.yml
  - turbo.json
autonomous: true
requirements_addressed: [API-01, API-02, OBS-02]
---

# Plan 02-01: API Scaffold & Database Schema

<objective>
Bootstrap the `apps/api/` workspace with Fastify v5 + tRPC v11, Prisma ORM with PostgreSQL 16, and the complete database schema. Smoke-test tRPC adapter compatibility. Add Docker Compose for local Postgres. Wire into turbo pipeline.
</objective>

<tasks>

<task id="01-01" title="Create apps/api workspace with package.json">
<read_first>
- packages/tax-core/package.json (reference workspace package.json pattern)
- pnpm-workspace.yaml (verify apps/* glob)
- turbo.json (existing pipeline config)
</read_first>
<action>
Create `apps/api/package.json` with:
```json
{
  "name": "@taxvn/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:push": "prisma db push",
    "db:seed": "tsx prisma/seed.ts",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@trpc/server": "^11.0.0",
    "fastify": "^5.0.0",
    "@fastify/cors": "^11.0.0",
    "@fastify/rate-limit": "^10.0.0",
    "@fastify/oauth2": "^8.0.0",
    "@prisma/client": "^6.0.0",
    "zod": "^3.24.0",
    "bcrypt": "^5.1.0",
    "nanoid": "^5.0.0",
    "jsonwebtoken": "^9.0.0",
    "pino": "^9.0.0"
  },
  "devDependencies": {
    "@types/bcrypt": "^5.0.0",
    "@types/jsonwebtoken": "^9.0.0",
    "@types/node": "^22.0.0",
    "prisma": "^6.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.9.0"
  }
}
```

Create `apps/api/tsconfig.json` with:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "outDir": "dist",
    "rootDir": "src",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true
  },
  "include": ["src/**/*.ts"],
  "exclude": ["node_modules", "dist"]
}
```

Update `turbo.json` to add api-specific tasks:
- `"db:generate"` task with no outputs
- `"db:migrate"` task with no outputs
</action>
<acceptance_criteria>
- `apps/api/package.json` contains `"name": "@taxvn/api"`
- `apps/api/package.json` contains `"fastify": "^5.0.0"`
- `apps/api/package.json` contains `"@trpc/server": "^11.0.0"`
- `apps/api/package.json` contains `"@prisma/client": "^6.0.0"`
- `apps/api/tsconfig.json` contains `"target": "ES2022"`
- `apps/api/tsconfig.json` contains `"module": "NodeNext"`
- `pnpm install` from root succeeds
</acceptance_criteria>
</task>

<task id="01-02" title="Define Prisma schema with all 5 tables">
<read_first>
- .planning/REQUIREMENTS.md (API-02 schema draft lines 26-31)
- .planning/phases/02-backend-api/02-CONTEXT.md (data model decisions)
</read_first>
<action>
Create `apps/api/prisma/schema.prisma`:
```prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id           String   @id @default(cuid())
  email        String   @unique
  passwordHash String?  @map("password_hash")
  name         String?
  googleId     String?  @unique @map("google_id")
  createdAt    DateTime @default(now()) @map("created_at")
  updatedAt    DateTime @updatedAt @map("updated_at")

  sessions           Session[]
  calculationHistory CalculationHistory[]
  shareSnapshots     ShareSnapshot[]
  pushTokens         PushToken[]

  @@map("users")
}

model Session {
  id               String   @id @default(cuid())
  userId           String   @map("user_id")
  refreshTokenHash String   @map("refresh_token_hash")
  deviceInfo       String?  @map("device_info")
  expiresAt        DateTime @map("expires_at")
  createdAt        DateTime @default(now()) @map("created_at")

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@index([expiresAt])
  @@map("sessions")
}

model CalculationHistory {
  id              String   @id @default(cuid())
  userId          String   @map("user_id")
  toolName        String   @map("tool_name")
  inputJson       Json     @map("input_json")
  resultJson      Json     @map("result_json")
  snapshotVersion Int      @map("snapshot_version")
  taxCoreVersion  String   @map("tax_core_version")
  createdAt       DateTime @default(now()) @map("created_at")

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId, createdAt(sort: Desc)])
  @@index([userId, toolName])
  @@map("calculation_history")
}

model ShareSnapshot {
  id              String    @id @default(cuid())
  token           String    @unique @db.VarChar(8)
  snapshotJson    Json      @map("snapshot_json")
  snapshotVersion Int       @map("snapshot_version")
  taxCoreVersion  String    @map("tax_core_version")
  createdById     String?   @map("created_by")
  expiresAt       DateTime  @map("expires_at")
  createdAt       DateTime  @default(now()) @map("created_at")

  createdBy User? @relation(fields: [createdById], references: [id], onDelete: SetNull)

  @@index([token])
  @@index([expiresAt])
  @@map("share_snapshots")
}

model PushToken {
  id        String   @id @default(cuid())
  userId    String   @map("user_id")
  token     String
  platform  Platform
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, token])
  @@map("push_tokens")
}

enum Platform {
  ios
  android
}
```

Create `apps/api/.env.example`:
```
DATABASE_URL=postgresql://taxvn:taxvn_dev@localhost:5432/taxvn_dev
JWT_ACCESS_SECRET=change-me-access-secret-min-32-chars
JWT_REFRESH_SECRET=change-me-refresh-secret-min-32-chars
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
PORT=4000
```
</action>
<acceptance_criteria>
- `apps/api/prisma/schema.prisma` contains `model User`
- `apps/api/prisma/schema.prisma` contains `model Session`
- `apps/api/prisma/schema.prisma` contains `model CalculationHistory`
- `apps/api/prisma/schema.prisma` contains `model ShareSnapshot`
- `apps/api/prisma/schema.prisma` contains `model PushToken`
- `apps/api/prisma/schema.prisma` contains `enum Platform`
- `apps/api/prisma/schema.prisma` contains `provider = "postgresql"`
- `apps/api/.env.example` contains `DATABASE_URL=`
- `npx prisma validate` exits 0 from apps/api/
</acceptance_criteria>
</task>

<task id="01-03" title="Docker Compose PostgreSQL service">
<read_first>
- docker-compose.yml (existing file)
</read_first>
<action>
Update `docker-compose.yml` to add a `db` service:
```yaml
services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: taxvn
      POSTGRES_PASSWORD: taxvn_dev
      POSTGRES_DB: taxvn_dev
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U taxvn -d taxvn_dev"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
```
Keep existing services if any.
</action>
<acceptance_criteria>
- `docker-compose.yml` contains `image: postgres:16-alpine`
- `docker-compose.yml` contains `POSTGRES_USER: taxvn`
- `docker-compose.yml` contains `POSTGRES_DB: taxvn_dev`
- `docker-compose.yml` contains `pgdata:` volume definition
- `docker compose config` exits 0
</acceptance_criteria>
</task>

<task id="01-04" title="Fastify + tRPC v11 server bootstrap with health endpoints">
<read_first>
- .planning/phases/02-backend-api/02-RESEARCH.md (tRPC v11 adapter pattern)
- .planning/phases/02-backend-api/02-CONTEXT.md (observability decisions)
</read_first>
<action>
Create `apps/api/src/env.ts`:
```typescript
import { z } from 'zod';

const envSchema = z.object({
  DATABASE_URL: z.string().url(),
  JWT_ACCESS_SECRET: z.string().min(32),
  JWT_REFRESH_SECRET: z.string().min(32),
  GOOGLE_CLIENT_ID: z.string().optional(),
  GOOGLE_CLIENT_SECRET: z.string().optional(),
  PORT: z.coerce.number().default(4000),
  NODE_ENV: z.enum(['development', 'production', 'test']).default('development'),
});

export const env = envSchema.parse(process.env);
export type Env = z.infer<typeof envSchema>;
```

Create `apps/api/src/context.ts`:
```typescript
import { CreateFastifyContextOptions } from '@trpc/server/adapters/fastify';
import { PrismaClient } from '@prisma/client';

const prisma = new PrismaClient();

export function createContext({ req, res }: CreateFastifyContextOptions) {
  return { req, res, prisma };
}

export type Context = Awaited<ReturnType<typeof createContext>>;
```

Create `apps/api/src/trpc.ts`:
```typescript
import { initTRPC, TRPCError } from '@trpc/server';
import { type Context } from './context.js';

const t = initTRPC.context<Context>().create();

export const router = t.router;
export const publicProcedure = t.procedure;
export const middleware = t.middleware;
```

Create `apps/api/src/routers/health.ts`:
```typescript
import { router, publicProcedure } from '../trpc.js';

export const healthRouter = router({
  check: publicProcedure.query(() => {
    return { status: 'ok', timestamp: new Date().toISOString() };
  }),
  ready: publicProcedure.query(async ({ ctx }) => {
    try {
      await ctx.prisma.$queryRaw`SELECT 1`;
      return { status: 'ok', database: 'connected', timestamp: new Date().toISOString() };
    } catch {
      return { status: 'error', database: 'disconnected', timestamp: new Date().toISOString() };
    }
  }),
});
```

Create `apps/api/src/routers/index.ts`:
```typescript
import { router } from '../trpc.js';
import { healthRouter } from './health.js';

export const appRouter = router({
  health: healthRouter,
});

export type AppRouter = typeof appRouter;
```

Create `apps/api/src/server.ts`:
```typescript
import Fastify from 'fastify';
import cors from '@fastify/cors';
import { fastifyTRPCPlugin, FastifyTRPCPluginOptions } from '@trpc/server/adapters/fastify';
import { appRouter, type AppRouter } from './routers/index.js';
import { createContext } from './context.js';
import { env } from './env.js';

export async function buildServer() {
  const server = Fastify({
    logger: {
      level: env.NODE_ENV === 'production' ? 'info' : 'debug',
    },
    routerOptions: {
      maxParamLength: 5000,
    },
  });

  await server.register(cors, { origin: true });

  // Non-tRPC health endpoints for infrastructure monitoring
  server.get('/health', async () => ({ status: 'ok', timestamp: new Date().toISOString() }));
  server.get('/health/ready', async () => {
    const { PrismaClient } = await import('@prisma/client');
    const prisma = new PrismaClient();
    try {
      await prisma.$queryRaw`SELECT 1`;
      return { status: 'ok', database: 'connected', timestamp: new Date().toISOString() };
    } catch {
      return { status: 'error', database: 'disconnected', timestamp: new Date().toISOString() };
    } finally {
      await prisma.$disconnect();
    }
  });

  await server.register(fastifyTRPCPlugin, {
    prefix: '/trpc',
    trpcOptions: {
      router: appRouter,
      createContext,
      onError({ path, error }) {
        server.log.error({ path, error: error.message, code: error.code }, 'tRPC error');
      },
    } satisfies FastifyTRPCPluginOptions<AppRouter>['trpcOptions'],
  });

  return server;
}
```

Create `apps/api/src/index.ts`:
```typescript
import { buildServer } from './server.js';
import { env } from './env.js';

async function main() {
  const server = await buildServer();

  try {
    const address = await server.listen({ port: env.PORT, host: '0.0.0.0' });
    server.log.info({ address }, 'Server started');
  } catch (err) {
    server.log.error(err, 'Server failed to start');
    process.exit(1);
  }
}

main();
```
</action>
<acceptance_criteria>
- `apps/api/src/index.ts` contains `buildServer`
- `apps/api/src/server.ts` contains `fastifyTRPCPlugin`
- `apps/api/src/server.ts` contains `maxParamLength: 5000`
- `apps/api/src/server.ts` contains `/health`
- `apps/api/src/server.ts` contains `/health/ready`
- `apps/api/src/trpc.ts` contains `initTRPC.context<Context>()`
- `apps/api/src/routers/index.ts` contains `export type AppRouter`
- `apps/api/src/env.ts` contains `JWT_ACCESS_SECRET`
- `tsc --noEmit` exits 0 from apps/api/
</acceptance_criteria>
</task>

</tasks>

<verification>
After all tasks complete:
1. `pnpm install` from root succeeds
2. `docker compose up -d db` starts PostgreSQL
3. `cd apps/api && pnpm exec prisma migrate dev --name init` creates initial migration
4. `pnpm dev` from apps/api starts server at port 4000
5. `curl http://localhost:4000/health` returns `{"status":"ok",...}`
6. `curl http://localhost:4000/health/ready` returns `{"status":"ok","database":"connected",...}`
7. `curl http://localhost:4000/trpc/health.check` returns tRPC response with `status: "ok"`
</verification>

<must_haves>
- Fastify v5 + tRPC v11 adapter compatibility smoke-tested
- PostgreSQL 16 via Docker Compose
- All 5 database tables defined (users, sessions, calculation_history, share_snapshots, push_tokens)
- Health endpoints responding
- Prisma client generated and queryable
</must_haves>
