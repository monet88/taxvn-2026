---
phase: 04-calculator-screens
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - apps/mobile/stores/useCalculatorStore.ts
  - apps/mobile/components/calculators/
  - apps/mobile/app/(tabs)/
  - apps/mobile/utils/
autonomous: true
requirements:
  - UX-02
  - UX-03
  - UX-04
  - UX-06
---

<objective>
Build the shared calculator runtime for Phase 4: reusable calculator screen primitives, draft persistence, numeric input handling, and navigable tool detail routes.

Purpose: This plan turns the tool inventory into a working application framework capable of hosting many calculators without duplicating screen logic 42 times.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Expand calculator state for per-tool drafts</name>
  <files>apps/mobile/stores/useCalculatorStore.ts</files>
  <action>Refactor `useCalculatorStore` from a history-only holder into a phase-4 runtime store that can persist draft inputs keyed by tool slug, restore those drafts on reopen, and leave room for Phase 5 history syncing.</action>
  <done>Tool drafts survive app switches and can be restored per screen.</done>
</task>

<task type="auto">
  <name>Task 2: Create shared calculator primitives</name>
  <files>apps/mobile/components/calculators/</files>
  <action>Create the shared mobile calculator building blocks: screen scaffold, section card, numeric/currency input, result summary, empty result, comparison block, and action row.</action>
  <done>Multiple calculators can share one visual and interaction system.</done>
</task>

<task type="auto">
  <name>Task 3: Add navigable tool detail routes</name>
  <files>apps/mobile/app/(tabs)/</files>
  <action>Create route structure that lets the tab inventory screens open specific calculator detail screens while preserving the existing tab shell.</action>
  <done>The inventory screens lead to real tool routes instead of static cards.</done>
</task>

</tasks>

<success_criteria>
- Draft persistence exists per tool slug
- Shared calculator UI primitives exist
- Inventory screens can navigate to tool detail routes
</success_criteria>
