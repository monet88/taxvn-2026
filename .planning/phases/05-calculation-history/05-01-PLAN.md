---
phase: 05-calculation-history
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/stores/useHistoryStore.ts
  - apps/mobile/hooks/useAutoSave.ts
  - apps/mobile/stores/useCalculatorStore.ts
autonomous: true
requirements: [HIST-06, HIST-07]

must_haves:
  truths:
    - User input updates trigger auto-save after debounce
    - Failed saves are queued locally for offline retry
  artifacts:
    - path: apps/mobile/stores/useHistoryStore.ts
      provides: Local persistence and sync logic for calculation history
    - path: apps/mobile/hooks/useAutoSave.ts
      provides: Debounced hook to invoke the history save on parameter changes
  key_links:
    - from: apps/mobile/hooks/useAutoSave.ts
      to: apps/mobile/stores/useHistoryStore.ts
      via: Zustand state hook
---

<objective>
Implement the core data and syncing layer for the Calculation History feature. This captures user calculations incrementally into a robust offline-friendly queue before committing to the backend.

Purpose: We need resilient local state to avoid spamming the backend during typing while ensuring data survives offline execution.
Output: Core stores and debouncing logic.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-calculation-history/05-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Define History Zustand Store & Offline Queue</name>
  <files>apps/mobile/stores/useHistoryStore.ts</files>
  <action>
Create a new Zustand store `useHistoryStore` using Zustand's `persist` middleware (with AsyncStorage).
The state should hold:
- `historyItems`: Local cache of synced calculation history items.
- `pendingQueue`: Array of items waiting to be synced to the backend.
- Actions: `queueSave(item: Omit<HistoryItem, 'id'|'created_at'>)`, `syncQueue()`, `loadHistory()`.
The `syncQueue` function should iterate the pending items and execute trpc's (`trpc.history.save.useMutation`) logic, moving successful items from `pendingQueue` to `historyItems`. Include D-05 (subtle offline indicator) in the type definition if needed, although it'll just be derived by whether the item is in the pending queue.
  </action>
  <verify>
    <automated>cat apps/mobile/stores/useHistoryStore.ts | grep -E "persist|pendingQueue|syncQueue|queueSave"</automated>
  </verify>
  <done>Store exported and correctly typed against calculation_history schema.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement Debounced Auto-Save Hook</name>
  <files>apps/mobile/hooks/useAutoSave.ts, apps/mobile/stores/useCalculatorStore.ts</files>
  <action>
Create `useAutoSave.ts` hook that accepts dependencies (like the current draft inputs and output).
Use `setTimeout` to debounce continuous changes by 5 seconds (D-01 decision).
Clear the timeout on changes; on trigger, it invokes `queueSave` in the `useHistoryStore` and flags the current draft as 'saved' (if needed).
Hook into `onUnmount` (using `useEffect` cleanup) to immediately flush a pending save if the user navigates away.
Integrate this hook wherever draft computations land, or at the top level of `apps/mobile/app/(tabs)/index.tsx` (the main calculator).
  </action>
  <verify>
    <automated>test -f apps/mobile/hooks/useAutoSave.ts && grep setTimeout apps/mobile/hooks/useAutoSave.ts</automated>
  </verify>
  <done>AutoSave hook successfully flushes on debounce and unmount.</done>
</task>

</tasks>

<verification>
Ensure we have a robust mechanism to buffer rapid inputs and persist to AsyncStorage without locking the JS thread.
</verification>

<success_criteria>
`useHistoryStore` exists. `useAutoSave` exists with 5s debounce. No syntax errors in store creation.
</success_criteria>

<output>
After completion, create `.planning/phases/05-calculation-history/05-01-SUMMARY.md`
</output>
