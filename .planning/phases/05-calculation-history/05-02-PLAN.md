---
phase: 05-calculation-history
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - apps/mobile/app/(tabs)/history.tsx
  - apps/mobile/app/(tabs)/two.tsx
  - apps/mobile/components/History/HistoryList.tsx
  - apps/mobile/components/History/HistoryItem.tsx
  - apps/mobile/components/History/HistoryFilters.tsx
autonomous: true
requirements: [HIST-01, HIST-03, HIST-04]

must_haves:
  truths:
    - User can navigate to the history tab and view past calculations
    - History items render offline-cloud vs sync icons based on their local queue status
    - History list supports searching and date filtering
  artifacts:
    - path: apps/mobile/app/(tabs)/history.tsx
      provides: Route for history tab
    - path: apps/mobile/components/History/HistoryList.tsx
      provides: Grouped and sorted component presentation
  key_links: []
---

<objective>
Build the visual layout for the Calculation History feature. Replace the placeholder tab with a real interface populated from the data layer implemented in Plan 01.

Purpose: Allow users to view and sift through their previously auto-saved tax outcomes.
Output: Searchable and visually grouped history view.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-calculation-history/05-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate shell tab to true History Tab</name>
  <files>apps/mobile/app/(tabs)/two.tsx, apps/mobile/app/(tabs)/history.tsx, apps/mobile/app/(tabs)/_layout.tsx</files>
  <action>
1. Rename `two.tsx` to `history.tsx` (if `two.tsx` is meant to be the history tab, or modify existing `history.tsx`).
2. Update `_layout.tsx` to ensure the tabs navigate correctly to `history` instead of `two` with appropriate Material or SF Symbols icon representing History.
3. Wire the page to load `historyItems` and `pendingQueue` from `useHistoryStore()`. Merge them sequentially so latest is on top.
  </action>
  <verify>
    <automated>grep -i "history" apps/mobile/app/\(tabs\)/_layout.tsx</automated>
  </verify>
  <done>History route is properly registered and renders with unified state from the store.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build Filter, Grouping, and Item Rendering</name>
  <files>apps/mobile/components/History/HistoryList.tsx, apps/mobile/components/History/HistoryItem.tsx, apps/mobile/components/History/HistoryFilters.tsx</files>
  <action>
1. Implement `HistoryFilters`: Provide a search bar (name/number) and discrete date/filter chips (e.g., using `react-native-ui`).
2. Implement `HistoryList`: Format the loaded state into temporal sections ("Today", "Yesterday", "Last Week", "Older") per decision D-03. Apply the current filter.
3. Implement `HistoryItem`: Present tool name, tax sums, and render a small dotted-cloud icon if the item comes from the `pendingQueue` (per D-05), or normal date string on success.
Wire everything together inside `history.tsx`.
  </action>
  <verify>
    <automated>test -f apps/mobile/components/History/HistoryItem.tsx && grep -E "Today|Yesterday|cloud" apps/mobile/components/History/HistoryList.tsx apps/mobile/components/History/HistoryItem.tsx</automated>
  </verify>
  <done>Visual presentation corresponds to requirements with proper temporal grouping.</done>
</task>

</tasks>

<verification>
We expect visual components to reliably render the output from `useHistoryStore` combined lists.
</verification>

<success_criteria>
Tab is fully functional, searching/filtering visually removes items without error. Components separated cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/05-calculation-history/05-02-SUMMARY.md`
</output>
