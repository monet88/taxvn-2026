---
phase: 1
plan: 3
title: "Fix isSecondHalf2026 flag and bracket inconsistency"
wave: 2
depends_on: [1]
files_modified:
  - packages/tax-core/src/taxDocumentGenerator.ts
  - packages/tax-core/src/multiSourceIncomeCalculator.ts
  - packages/tax-core/src/foreignerTaxCalculator.ts
  - packages/tax-core/src/taxCalculator.ts
  - packages/tax-core/src/incomeSummaryCalculator.ts
  - src/lib/snapshotTypes.ts
  - src/components/TaxDocumentGenerator/TaxDocumentGenerator.tsx
  - src/components/MultiSourceIncome/MultiSourceIncome.tsx
  - src/components/ForeignerTaxCalculator/ForeignerTaxCalculator.tsx
autonomous: true
requirements_addressed: [FOUND-04, FOUND-06]
must_haves:
  - isSecondHalf2026 flag removed from all files
  - New-law deductions applied uniformly for all of 2026
  - incomeSummaryCalculator and taxCalculator produce identical bracket results
---

<objective>
Remove the deprecated `isSecondHalf2026` flag from all 7 affected files and fix the bracket inconsistency between incomeSummaryCalculator and taxCalculator. After this, new-law deductions apply uniformly for the entire year 2026.
</objective>

<tasks>

<task id="3.1">
<title>Remove isSecondHalf2026 from tax-core calculator modules</title>
<read_first>
- packages/tax-core/src/foreignerTaxCalculator.ts (see line ~146 for deprecated flag)
- packages/tax-core/src/multiSourceIncomeCalculator.ts (see line ~179 for flag usage)
- packages/tax-core/src/taxDocumentGenerator.ts (see getDeductionAmounts function)
</read_first>
<action>
1. In `packages/tax-core/src/foreignerTaxCalculator.ts`:
   - Remove `isSecondHalf2026?: boolean` from the input interface (line ~146)
   - Remove any logic that uses this flag to select brackets/deductions
   - Ensure new-law brackets are used unconditionally for year >= 2026

2. In `packages/tax-core/src/multiSourceIncomeCalculator.ts`:
   - Remove `isSecondHalf2026?: boolean` from the input interface (line ~179)
   - Change line ~298 from `calculateProgressiveTax(monthlyTaxable, input.isSecondHalf2026 || false)` to `calculateProgressiveTax(monthlyTaxable, true)` (or remove the parameter entirely if the 2026+ date logic handles it)
   - Ensure new-law is applied uniformly for all of 2026

3. In `packages/tax-core/src/taxDocumentGenerator.ts` (NOTE: this file stays in web app, not extracted to tax-core. If it was extracted, fix there; otherwise fix in `src/lib/`):
   - In `getDeductionAmounts(year, isSecondHalf2026)`:
     - Remove `isSecondHalf2026` parameter
     - Change signature to `getDeductionAmounts(year: number)`
     - For `year >= 2026`, always return new-law deduction amounts (personal deduction 18M, dependent deduction 8.5M per dependent according to Law 109/2025/QH15)
</action>
<acceptance_criteria>
- `grep -r "isSecondHalf2026" packages/tax-core/src/` returns zero results
- `foreignerTaxCalculator.ts` input interface has no `isSecondHalf2026` field
- `multiSourceIncomeCalculator.ts` does not reference `isSecondHalf2026`
- `getDeductionAmounts` takes only `year: number` parameter
</acceptance_criteria>
</task>

<task id="3.2">
<title>Remove isSecondHalf2026 from snapshotTypes and components</title>
<read_first>
- src/lib/snapshotTypes.ts (search for isSecondHalf2026 — appears in type definitions and defaults)
- src/components/TaxDocumentGenerator/TaxDocumentGenerator.tsx (line ~67)
- src/components/MultiSourceIncome/MultiSourceIncome.tsx (line ~45)
- src/components/ForeignerTaxCalculator/ForeignerTaxCalculator.tsx (line ~116)
</read_first>
<action>
1. In `src/lib/snapshotTypes.ts`:
   - Remove `isSecondHalf2026: boolean` from all interfaces (found at lines ~59, ~134, ~340)
   - Remove `isSecondHalf2026: true` from all default state constants (found at lines ~69, ~140, ~523)
   - Add migration note in a comment: `// 2026-04-01: isSecondHalf2026 removed — new-law applies uniformly for all of 2026`

2. In `src/components/TaxDocumentGenerator/TaxDocumentGenerator.tsx`:
   - Remove line ~67: `const isSecondHalf2026 = year === 2026 && (month === undefined || month >= 7);`
   - Change line ~68: `return getDeductionAmounts(year, isSecondHalf2026)` → `return getDeductionAmounts(year)`

3. In `src/components/MultiSourceIncome/MultiSourceIncome.tsx`:
   - Remove line ~45: `isSecondHalf2026: tabState.isSecondHalf2026,` from the object passed to the calculator

4. In `src/components/ForeignerTaxCalculator/ForeignerTaxCalculator.tsx`:
   - Remove line ~116: `isSecondHalf2026: tabState.isSecondHalf2026 ?? true,` from the object passed to the calculator
</action>
<acceptance_criteria>
- `grep -r "isSecondHalf2026" src/` returns zero results
- `src/lib/snapshotTypes.ts` contains the migration comment
- The three component files compile without TypeScript errors after the field removal
</acceptance_criteria>
</task>

<task id="3.3">
<title>Fix bracket inconsistency between incomeSummaryCalculator and taxCalculator</title>
<read_first>
- packages/tax-core/src/incomeSummaryCalculator.ts (the full file)
- packages/tax-core/src/taxCalculator.ts (search for calculateNewTax, calculateOldTax, bracket definitions)
</read_first>
<action>
1. Read both files completely to identify the exact inconsistency:
   - `taxCalculator.ts` defines `OLD_TAX_BRACKETS` (7 brackets) and `NEW_TAX_BRACKETS` (5 brackets)
   - `incomeSummaryCalculator.ts` may define its own bracket arrays or use different calculation logic

2. Fix incomeSummaryCalculator to use the SAME bracket arrays from taxCalculator:
   - Import `OLD_TAX_BRACKETS`, `NEW_TAX_BRACKETS`, `calculateOldTax`, `calculateNewTax` from `./taxCalculator` (or `./constants`)
   - Remove any duplicated bracket definitions
   - Ensure the calculation logic delegates to the same functions used by taxCalculator

3. Write a quick verification: for input `monthlyIncome = 30_000_000`, both calculators should return identical bracket breakdowns.
</action>
<acceptance_criteria>
- `incomeSummaryCalculator.ts` imports bracket arrays from `./taxCalculator` or `./constants` — does NOT define its own
- No duplicated bracket definitions exist in `packages/tax-core/src/`
- Running the test suite (Plan 04) will verify both produce identical results for same input
</acceptance_criteria>
</task>

</tasks>

<verification>
After all tasks complete:
1. `grep -r "isSecondHalf2026" packages/ src/` returns zero results
2. `pnpm --filter @taxvn/tax-core build` succeeds
3. The existing Next.js app builds without TypeScript errors (`pnpm build`)
</verification>
