# Phase 3.2: Backend Integration & Rate Limiting - Context

**Gathered:** 2026-04-01
**Status:** Ready for planning
**Source:** Gap Closure (Audit)

<domain>
## Phase Boundary

Resolve integration failures identified in the Phase 02 audit. Specifically, configure Google OAuth in the Supabase local environment (`supabase/config.toml`) and correctly enforce the 5 attempts per 15 min auth sign-in rate limit.

</domain>

<decisions>
## Implementation Decisions

### Configuration Updates
- **D-01:** Update `supabase/config.toml` to activate `auth.external.google`. Set placeholder attributes (`client_id`, `secret`, `redirect_uri`).
- **D-02:** Update the `sign_in_sign_ups` setting in the `[auth]` section of `supabase/config.toml` to accurately reflect a rate limit of 5 attempts per 15 minutes. Note that Supabase handles the exponential backoff natively for subsequent requests.

### Agent's Discretion
None - straightforward configuration updates mapped directly from the audit.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Audits and Workflows
- `v1.0-MILESTONE-AUDIT.md` — The audit findings marking API-04 and SEC-01 as gaps.

### Configuration
- `supabase/config.toml` — The target configuration block for Supabase Auth.

</canonical_refs>

<specifics>
## Specific Ideas

Supabase configuration uses seconds for interval parameters in rate limiting. The limit is 5 attempts per 15 minutes (900 seconds) — ensure the configuration is translated accurately according to GoTrue's rate limit parameters.

</specifics>

<deferred>
## Deferred Ideas

- Full functional QA of the OAuth flow with actual Google Client IDs (requires staging environment; only the platform readiness/config block is scoped for this phase).

</deferred>

---

*Phase: 03.2-backend-integration*
*Context gathered: 2026-04-01 via gap closure*
