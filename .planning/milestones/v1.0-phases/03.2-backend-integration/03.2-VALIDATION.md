# Phase 03.2: Backend Integration & Rate Limiting - Validation

<dimensions>
1. **Goal Backward:** Does the configuration resolve the audit gap?
   - Phase Goal: Add missing Google OAuth and enforce 5/15min auth sign up/in rate limits.
   - Validation: Validate `supabase/config.toml` contains `[auth.external.google]` block, `enabled = true`. Ensure login limits reflect 5 tokens per 900s.

2. **Nyquist Rate:** Is the check performed at runtime?
   - Validation: Run a script utilizing the Supabase CLI to `supabase start` and ping local login limits iteratively through HTTP requests. Or rely on Static parsing. Let's do static check + `supabase status` log check if local backend is available.

3. **Codebase Impact:** Do changes break current auth?
   - Validation: Test standard email auth to ensure OAuth config addition doesn't error out GoTrue.

4. **Rollback Strategy:** Can this be gracefully reverted?
   - Validation: Git stash/revert `config.toml` changes.

5. **Failure Modes:** How does the approach handle errors, retries, constraints?
   - Validation: If `client_id` is missing, GoTrue throws config error on `supabase start`. Placeholder keys MUST be valid strings (`""`).

6. **Edge Cases:** What boundaries/extremes must be tested?
   - Validation: Verify exact threshold behavior. Attempt 6 logins within 15 mins to confirm failure (429 status code) on 6th attempt.

7. **Observability:** How will we know it works in production?
   - Validation: Cloud dashboard configuration will match the `config.toml`.
</dimensions>

<tests>
## Required Automated Tests
- Integration tests or bash scripts to read the contents of `supabase/config.toml` verifying limits configuration parameters visually.
</tests>

<manual>
## Required Manual UAT
1. Ensure `supabase start` completes without GoTrue failing to initialize.
2. Send 6 explicit login POST requests via curl to GoTrue local endpoint confirming the 6th receives HTTP 429 Error.
</manual>

---
*Created: 2026-04-01*
