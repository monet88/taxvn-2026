---
phase: 1
plan: 4
title: "Golden-output test suite for all calculators"
wave: 3
depends_on: [2, 3]
files_modified:
  - packages/tax-core/src/__tests__/taxCalculator.test.ts
  - packages/tax-core/src/__tests__/grossNetCalculator.test.ts
  - packages/tax-core/src/__tests__/bonusCalculator.test.ts
  - packages/tax-core/src/__tests__/esopCalculator.test.ts
  - packages/tax-core/src/__tests__/overtimeCalculator.test.ts
  - packages/tax-core/src/__tests__/annualSettlementCalculator.test.ts
  - packages/tax-core/src/__tests__/foreignerTaxCalculator.test.ts
  - packages/tax-core/src/__tests__/securitiesTaxCalculator.test.ts
  - packages/tax-core/src/__tests__/realEstateTransferTaxCalculator.test.ts
  - packages/tax-core/src/__tests__/incomeSummaryCalculator.test.ts
  - packages/tax-core/src/__tests__/bracketConsistency.test.ts
  - packages/tax-core/src/__tests__/grossNetRoundTrip.test.ts
autonomous: true
requirements_addressed: [FOUND-03]
must_haves:
  - 120+ test cases across all 40+ calculators
  - Each calculator has at least 3 tests (happy path, boundary, edge case)
  - GROSS↔NET round-trip test returns same value within 1 VND
  - Bracket consistency test passes (incomeSummaryCalculator == taxCalculator)
  - All tests pass with pnpm test
---

<objective>
Write a comprehensive golden-output test suite using Vitest for ALL calculator modules in packages/tax-core. Each calculator must have at least 3 test cases (happy path + boundary + edge case), totaling 120+ tests. Include specific tests for GROSS↔NET round-trip accuracy and bracket consistency between incomeSummaryCalculator and taxCalculator.
</objective>

<tasks>

<task id="4.1">
<title>Write tests for core tax calculator functions</title>
<read_first>
- packages/tax-core/src/taxCalculator.ts (calculateOldTax, calculateNewTax, calculateTaxForDate)
- packages/tax-core/src/constants.ts (bracket definitions)
</read_first>
<action>
Create `packages/tax-core/src/__tests__/taxCalculator.test.ts`:

Tests must cover:
1. `calculateOldTax(monthlyIncome)` — 7-bracket system:
   - Happy: income 30,000,000 → verify exact tax amount
   - Boundary: income at each bracket threshold (5M, 10M, 18M, 32M, 52M, 80M)
   - Edge: income 0 → tax 0
   - Edge: very high income (200,000,000)

2. `calculateNewTax(monthlyIncome)` — 5-bracket system:
   - Happy: income 30,000,000 → verify exact tax amount
   - Boundary: income at each bracket threshold
   - Edge: income 0 → tax 0
   - Edge: negative income → handles gracefully

3. `calculateTaxForDate(income, date)` — date-aware selection:
   - Before 2026: uses old brackets
   - After 2026-01-01: uses new brackets

4. Insurance calculation:
   - Happy: standard salary 30M
   - Boundary: salary > cap (46.8M BHXH cap)
   - Edge: salary = 0

Each test uses `expect(result).toBeCloseTo(expectedValue, 0)` for VND amounts (integer precision).
</action>
<acceptance_criteria>
- `taxCalculator.test.ts` exists with at least 15 test cases
- Tests cover both old and new bracket systems
- All tests pass: `pnpm --filter @taxvn/tax-core test -- --run taxCalculator`
</acceptance_criteria>
</task>

<task id="4.2">
<title>Write GROSS↔NET round-trip test</title>
<read_first>
- packages/tax-core/src/grossNetCalculator.ts (binary search logic, precision parameter)
</read_first>
<action>
Create `packages/tax-core/src/__tests__/grossNetRoundTrip.test.ts`:

1. GROSS→NET→GROSS round-trip:
   - Input: GROSS = 25,000,000
   - Call grossToNet(25000000) → get NET
   - Call netToGross(NET) → get GROSS_RESULT
   - Assert: `Math.abs(25000000 - GROSS_RESULT) <= 1` (within 1 VND)

2. Test at multiple income levels:
   - 10,000,000 (low)
   - 30,000,000 (mid)
   - 50,000,000 (high)
   - 100,000,000 (very high)

3. NET→GROSS→NET round-trip:
   - Same pattern in reverse

4. Edge cases:
   - GROSS = 0 → NET = 0
   - GROSS below personal deduction → NET = GROSS (no tax)
</action>
<acceptance_criteria>
- `grossNetRoundTrip.test.ts` exists with at least 8 test cases
- All round-trip tests pass within 1 VND tolerance
- Test logs actual vs expected for verification
</acceptance_criteria>
</task>

<task id="4.3">
<title>Write bracket consistency test</title>
<read_first>
- packages/tax-core/src/incomeSummaryCalculator.ts
- packages/tax-core/src/taxCalculator.ts
</read_first>
<action>
Create `packages/tax-core/src/__tests__/bracketConsistency.test.ts`:

1. For each of these incomes: 10M, 20M, 30M, 50M, 80M, 120M:
   - Calculate via taxCalculator (calculateOldTax + calculateNewTax)
   - Calculate via incomeSummaryCalculator (however it exposes its calculation)
   - Assert both produce identical total tax amounts
   - Assert both produce identical bracket breakdowns (amount per bracket)

2. Parameterized test using `describe.each` or `test.each` for multiple inputs
</action>
<acceptance_criteria>
- `bracketConsistency.test.ts` exists with at least 6 test cases (one per income level)
- All tests pass: incomeSummaryCalculator matches taxCalculator exactly
</acceptance_criteria>
</task>

<task id="4.4">
<title>Write tests for remaining 25+ calculator modules</title>
<read_first>
- packages/tax-core/src/bonusCalculator.ts
- packages/tax-core/src/esopCalculator.ts
- packages/tax-core/src/overtimeCalculator.ts
- packages/tax-core/src/foreignerTaxCalculator.ts
- packages/tax-core/src/securitiesTaxCalculator.ts
</read_first>
<action>
Create test files for EACH remaining calculator module. For each calculator, write at minimum 3 tests:
- **Happy path**: typical input → verify expected output
- **Boundary**: edge of a bracket/threshold/cap → verify correct behavior
- **Edge case**: zero input, missing optional fields, or unusual combination

Calculator test files to create (each as `packages/tax-core/src/__tests__/<name>.test.ts`):
- bonusCalculator.test.ts (3+ tests)
- esopCalculator.test.ts (3+ tests)
- overtimeCalculator.test.ts (3+ tests)
- annualSettlementCalculator.test.ts (3+ tests)
- foreignerTaxCalculator.test.ts (3+ tests)
- securitiesTaxCalculator.test.ts (3+ tests)
- realEstateTransferTaxCalculator.test.ts (3+ tests)
- rentalIncomeTaxCalculator.test.ts (3+ tests)
- householdBusinessTaxCalculator.test.ts (3+ tests)
- pensionCalculator.test.ts (3+ tests)
- contentCreatorTaxCalculator.test.ts (3+ tests)
- cryptoTaxCalculator.test.ts (3+ tests)
- coupleTaxOptimizer.test.ts (3+ tests)
- multiSourceIncomeCalculator.test.ts (3+ tests)
- freelancerCalculator.test.ts (3+ tests)
- salaryComparisonCalculator.test.ts (3+ tests)
- withholdingTaxCalculator.test.ts (3+ tests)
- vatCalculator.test.ts (3+ tests)
- severanceCalculator.test.ts (3+ tests)
- latePaymentCalculator.test.ts (3+ tests)
- inheritanceGiftTaxCalculator.test.ts (3+ tests)
- businessFormComparisonCalculator.test.ts (3+ tests)
- mortgageCalculator.test.ts (3+ tests)
- monthlyPlannerCalculator.test.ts (3+ tests)
- taxPlanningSimulator.test.ts (3+ tests)
- yearlyTaxCalculator.test.ts (3+ tests)

Total: 26 files × 3+ tests = 78+ tests from this task alone.

For each test, read the calculator's exported functions and their input interfaces. Use realistic Vietnamese tax data (salary ranges 5M-100M VND, typical deductions, standard insurance rates).
</action>
<acceptance_criteria>
- 26 test files exist in `packages/tax-core/src/__tests__/`
- Each file has at least 3 test cases
- `pnpm --filter @taxvn/tax-core test` runs 120+ tests total with zero failures
</acceptance_criteria>
</task>

</tasks>

<verification>
After all tasks complete:
1. `pnpm --filter @taxvn/tax-core test` runs with zero failures
2. Total test count is 120+ (verify from vitest output)
3. GROSS↔NET round-trip passes within 1 VND
4. Bracket consistency tests pass (incomeSummaryCalculator == taxCalculator)
</verification>
