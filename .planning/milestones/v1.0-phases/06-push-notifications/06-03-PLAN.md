---
wave: 2
depends_on:
  - 06-01
files_modified:
  - supabase/functions/send_law_change_push/index.ts
  - supabase/migrations/20260402_law_change_trigger.sql
autonomous: false
---

# Plan: 06-03 - Remote Law Change Alerts

<objective>
Tích hợp Supabase Edge Function đọc cài đặt setting của từng user và bắn Firebase Admin message để trigger remote push notifications liên quan tới thay đổi luật (Law Changes).
</objective>

<requirements_addressed>
- PUSH-02: Remote push notification cho thay đổi luật thuế
</requirements_addressed>

<tasks>
<task>
<action>Tạo webhook/Edge Function `send_law_change_push` để gửi thông báo bằng Firebase Admin SDK tới thiết bị.</action>
<read_first>
- .planning/phases/06-push-notifications/06-CONTEXT.md
</read_first>
<acceptance_criteria>
- `supabase/functions/send_law_change_push/index.ts` tồn tại.
- Code dùng Firebase Admin Node SDK (service account/inject cert).
- Chỉ lấy các records `push_tokens` nếu user tương ứng có cài đặt `notification_settings->>'law_changes' = true`.
- Batch gửi MulticastMessage qua Firebase.
</acceptance_criteria>
</task>

<task>
<action>Set up DB Trigger để khi có bản thu thập mới ở bảng `tax_law_history` (nếu có), edge function sẽ được trigger.</action>
<read_first>
- supabase/migrations/20260402_law_change_trigger.sql
</read_first>
<acceptance_criteria>
- Một Postgres trigger webhook gọi sang Supabase Edge Function nội bộ mỗi khi luật thay đổi, đảm bảo tự động broadcast.
</acceptance_criteria>
</task>
</tasks>
