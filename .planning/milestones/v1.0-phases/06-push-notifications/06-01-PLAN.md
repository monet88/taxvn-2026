---
wave: 1
depends_on:
files_modified:
  - apps/mobile/package.json
  - apps/mobile/app.config.ts
  - apps/mobile/src/store/userStore.ts
  - apps/mobile/src/hooks/usePushNotifications.ts
  - supabase/migrations/20260402_push_tokens.sql
  - apps/mobile/src/app/(tabs)/tools/[slug].tsx
  - apps/mobile/src/app/(tabs)/account/index.tsx
autonomous: false
---

# Plan: 06-01 - Notification Infrastructure & Permissions

<objective>
Thiết lập cơ sở hạ tầng Push Notification (cả local và remote qua FCM), cập nhật DB lưu trữ cài đặt & push token của user, và hiển thị luồng xin quyền Push sau lần tính toán đầu tiên.
</objective>

<requirements_addressed>
- PUSH-03: In-app notification settings (toggle từng category)
- PUSH-04: Permission request sau lần tính thuế đầu tiên
- API-08: Push notification token registry (lưu FCM/APNs token per user per device)
</requirements_addressed>

<tasks>
<task>
<action>Tạo file migration Supabase để thêm bảng `push_tokens` và thêm cột config notification vào `users`.</action>
<read_first>
- .planning/phases/06-push-notifications/06-CONTEXT.md
</read_first>
<acceptance_criteria>
- File migration sql tạo bảng `push_tokens` (id, user_id, token, device_id, platform, created_at, updated_at) có ràng buộc unique trên (user_id, device_id)
- Có lệnh `ALTER TABLE users ADD COLUMN notification_settings JSONB DEFAULT '{"deadlines": true, "law_changes": true}'::jsonb;`
- Row Level Security policies cho `push_tokens` được phân quyền chính xác.
</acceptance_criteria>
</task>

<task>
<action>Cài đặt Firebase/Expo Push trong React Native và tạo Custom Hook `usePushNotifications` để lấy FCM token và lắng nghe permission.</action>
<read_first>
- apps/mobile/package.json
</read_first>
<acceptance_criteria>
- Sử dụng `@react-native-firebase/messaging` hoặc Expo cho Firebase.
- Hàm trả về `token`, `permissionStatus`.
- Lắng nghe event `onTokenRefresh` để update server.
</acceptance_criteria>
</task>

<task>
<action>Mở rộng Zustand userStore lưu `notificationSettings` (local & sync lên Backend) và gọi API Upsert `push_tokens` bằng tRPC/Supabase client.</action>
<read_first>
- apps/mobile/src/store/userStore.ts
</read_first>
<acceptance_criteria>
- Zustand store có state `notificationSettings`
- Có function gọi async để sync state này lên Supabase auth.users hoặc rpc.
- Mỗi lần khởi động mạng thành công có FCM token, gọi API (hoặc supabase auth client) lưu vào bảng `push_tokens`.
</acceptance_criteria>
</task>

<task>
<action>Implement luồng xin quyền Push (PUSH-04) ở trang kết quả máy tính (`[slug].tsx`).</action>
<read_first>
- apps/mobile/src/app/(tabs)/tools/[slug].tsx
</read_first>
<acceptance_criteria>
- Khi tính thuế lần đầu tiên (hoặc khi save logic), dùng async storage flag check `has_asked_push`
- Xin quyền sử dụng Custom alert với nội dung "Nhấn cho phép để nhận nhắc deadline..."
- Nút "Cho phép" kích hoạt OS Prompt. Lưu kết quả setting.
</acceptance_criteria>
</task>

<task>
<action>Update màn hình Cài đặt (`account/index.tsx`) thêm 2 toggle: "Nhắc deadline thuế" và "Nhắc luật thay đổi". Cảnh báo soft-prompt nếu OS chặn.</action>
<read_first>
- apps/mobile/src/app/(tabs)/account/index.tsx
</read_first>
<acceptance_criteria>
- 2 the toggle bind tới Zustand store, trigger sync backend
- Nếu `permissionStatus` bị OS chặn, hiện thông báo "Soft-prompt banner" hướng dẫn mở System Settings
</acceptance_criteria>
</task>
</tasks>
