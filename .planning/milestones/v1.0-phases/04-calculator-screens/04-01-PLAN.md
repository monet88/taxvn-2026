---
phase: 04-calculator-screens
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-calculator-screens/04-CONTEXT.md
  - apps/mobile/nativewind-env.d.ts
  - apps/mobile/tsconfig.json
  - apps/mobile/utils/secureStore.ts
  - apps/mobile/components/ExternalLink.tsx
  - apps/mobile/constants/toolRegistry.ts
  - apps/mobile/components/ToolListScreen.tsx
  - apps/mobile/app/(tabs)/index.tsx
  - apps/mobile/app/(tabs)/so-sanh.tsx
  - apps/mobile/app/(tabs)/tham-khao.tsx
autonomous: true
requirements:
  - UX-03
  - UX-04
  - UX-06
  - CALC-01
  - CALC-42
---

<objective>
Repair the mobile calculator baseline and replace the blank tab placeholders with a real 42-tool inventory surface.

Purpose: This is the phase-start gating slice. It removes the NativeWind/TypeScript blocker, establishes the mobile tool registry as the Phase 4 source of truth, and makes the three calculator tabs reflect the actual product scope instead of empty placeholders.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-calculator-screens/04-CONTEXT.md
@.planning/phases/04-calculator-screens/04-UI-SPEC.md
@.planning/phases/03-mobile-foundation/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the mobile TypeScript baseline</name>
  <files>apps/mobile/nativewind-env.d.ts, apps/mobile/tsconfig.json, apps/mobile/utils/secureStore.ts, apps/mobile/components/ExternalLink.tsx</files>
  <action>Add the missing NativeWind type declaration, include it in the mobile tsconfig, make the secure storage adapter structurally compatible with both Zustand and Supabase Auth, and remove any obsolete type suppression that blocks a clean compile.</action>
  <verify>
    <automated>apps/mobile/node_modules/.bin/tsc.CMD --noEmit</automated>
  </verify>
  <done>The mobile shell compiles without `className` prop failures.</done>
</task>

<task type="auto">
  <name>Task 2: Create the Phase 4 tool registry</name>
  <files>apps/mobile/constants/toolRegistry.ts</files>
  <action>Create a typed registry for all 42 Phase 4 tools. Each entry must include: slug, requirement ID, title, short description, and owning mobile group (`calculate`, `compare`, or `reference`). The registry must be complete even if every detail screen is not implemented yet.</action>
  <verify>
    <automated>Registry exports 42 tools and all three groups are non-empty.</automated>
  </verify>
  <done>The mobile app has a single source of truth for the calculator inventory.</done>
</task>

<task type="auto">
  <name>Task 3: Replace blank tab screens with searchable inventory screens</name>
  <files>apps/mobile/components/ToolListScreen.tsx, apps/mobile/app/(tabs)/index.tsx, apps/mobile/app/(tabs)/so-sanh.tsx, apps/mobile/app/(tabs)/tham-khao.tsx</files>
  <action>Build a reusable list screen that reads the registry, supports local search, and renders calm card-based inventory UI for each content tab. Each tab must show the right subset of tools and the visible total should match the registry.</action>
  <verify>
    <automated>apps/mobile/node_modules/.bin/tsc.CMD --noEmit</automated>
  </verify>
  <done>The three content tabs expose the actual Phase 4 scope instead of placeholder text.</done>
</task>

</tasks>

<success_criteria>
- `apps/mobile` TypeScript compiles cleanly
- The mobile tool registry lists all 42 Phase 4 tools
- `Tính toán`, `So sánh`, and `Tham khảo` render searchable tool inventory screens
</success_criteria>

<output>
After completion, create `.planning/phases/04-calculator-screens/04-01-SUMMARY.md`
</output>
