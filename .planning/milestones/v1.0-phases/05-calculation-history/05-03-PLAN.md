---
phase: 05-calculation-history
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - apps/mobile/app/(tabs)/history.tsx
  - apps/mobile/components/History/HistoryList.tsx
  - apps/mobile/components/History/HistoryItem.tsx
  - apps/mobile/stores/useHistoryStore.ts
autonomous: true
requirements: [HIST-02, HIST-05]

must_haves:
  truths:
    - User can restore history item to current calculator draft state
    - User can delete single items
    - User can bulk delete items using long-press multi-select Mode
  artifacts:
    - path: apps/mobile/components/History/HistoryList.tsx
      provides: Multi-select state tracking and deletion dispatch
  key_links: []
---

<objective>
Implement interactive behaviors for calculation history: restoring state seamlessly and offering both single-item swipe-delete and multi-item long-press delete workflows.

Purpose: Bring mutability and actionability to the history feature. Users must be able to resume a calculation or prune their traces.
Output: Integrated action flows that bridge the History list back to the active computations or execute remote deletion.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-calculation-history/05-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add Restoring Capability to History Items</name>
  <files>apps/mobile/app/(tabs)/history.tsx, apps/mobile/components/History/HistoryItem.tsx</files>
  <action>
1. Enhance the `<HistoryItem />` tap-action (using `onPress`).
2. Tapping it will extract the stored `input_json` object.
3. Call the `useCalculatorStore` to set the drafts for that target tool without any confirmation dialogs (decision D-02).
4. Afterwards, trigger navigation (using `router.navigate()`) to the calculator path that evaluates this tool type so the user can immediately see and continue where they left off.
  </action>
  <verify>
    <automated>grep onPress apps/mobile/components/History/HistoryItem.tsx</automated>
  </verify>
  <done>Restoration sets store and navigates gracefully.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build Bulk Selection & Swipe Delete Workflows</name>
  <files>apps/mobile/app/(tabs)/history.tsx, apps/mobile/components/History/HistoryList.tsx</files>
  <action>
1. Define actions in `useHistoryStore` for deletion: `removeItems(ids: string[])` that will trigger `trpc.history.delete.useMutation`.
2. Introduce a `isMultiSelectMode` and `selectedIds` state array in the `history.tsx` page.
3. Upon long-press on any `HistoryItem` (`onLongPress`), activate multi-select mode (Decision D-04). Render a checkbox next to all items and a fixed bottom Action Bar containing a 'Delete X items' button.
4. Support standard single deletion via a swipeable action standard in React Native (e.g., using `react-native-gesture-handler` or basic PanResponder logic if feasible, or just provide a trash icon in multi-select for everything to keep UI minimal if swiping entails major overhead).
  </action>
  <verify>
    <automated>grep -E "removeItems|selectedIds" apps/mobile/app/\(tabs\)/history.tsx apps/mobile/stores/useHistoryStore.ts</automated>
  </verify>
  <done>Long-press triggers deletion mode enabling bulk history cleanup.</done>
</task>

</tasks>

<verification>
We expect tapping an item to replace state in `useCalculatorStore` and deleting them to synchronize removal back to the DB immediately.
</verification>

<success_criteria>
Store actions correctly prune items and trigger async mutations. UI provides a clear bottom sheet/action bar deleting multi-selected targets.
</success_criteria>

<output>
After completion, create `.planning/phases/05-calculation-history/05-03-SUMMARY.md`
</output>
